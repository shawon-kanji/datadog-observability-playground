apiVersion: v1
kind: Service
metadata:
  name: dd-app-api-service
  labels:
    app: dd-app-api-service
spec:
  type: NodePort
  selector:
      app: dd-app-api
  ports:
    - protocol: TCP
      name: http
      nodePort: 30000
      port: 3000
      targetPort: 3000